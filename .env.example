# ===================================
# AGRICHAINMARKET BACKEND .env.example
# ===================================
# Copy file này thành .env và điền các giá trị thực tế
# DO NOT commit actual .env file to git!

# ============ SERVER ============
PORT=3000

# ============ DATABASE (PostgreSQL) ============
DB_USER=postgres
DB_HOST=localhost
DB_NAME=do_an_chuyen_nganh
DB_PASSWORD=your_postgres_password_here
DB_PORT=5432

# ============ AUTHENTICATION ============
# IMPORTANT: Tạo secret key mạnh cho production!
# Dùng lệnh: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345
JWT_EXPIRE=24h

# ============ BLOCKCHAIN (Polygon Amoy Testnet) ============
AMOY_RPC=https://rpc-amoy.polygon.technology
PRIVATE_KEY=0xyour_wallet_private_key_here
CONTRACT_ADDRESS=0xyour_deployed_contract_address_here

# ============ EXTERNAL APIs ============

# Google Gemini API (Chatbot)
# Miễn phí: 60 requests/phút
# Lấy từ: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Email Configuration (Gmail)
# Dùng App Password thay vì password thực
# Tạo tại: https://myaccount.google.com/apppasswords
EMAIL_USER=your_gmail@gmail.com
EMAIL_PASSWORD=your_app_password_here

# ============ PAYMENT GATEWAY (MoMo) ============
# Test Sandbox Account
MOMO_PARTNER_CODE=MOMO
MOMO_ACCESS_KEY=F8BBA842ECF85
MOMO_SECRET_KEY=your_momo_secret_key_here
MOMO_REDIRECT_URL=http://localhost:5173/payment/success
MOMO_IPN_URL=https://your-public-domain.com/api/payments/momo/callback

# ============ ENVIRONMENT ============
NODE_ENV=development
# development | production
